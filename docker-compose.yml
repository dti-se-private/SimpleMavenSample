services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    entrypoint: [ "/bin/sh", "-c" ]
    network_mode: host
    environment:
      - APP_PORT=${APP_PORT}
    command:
      - |-
        java -jar ./ch-simple/simple/target/simple-0.8-SNAPSHOT.jar