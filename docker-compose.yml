services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    entrypoint: [ "/bin/sh", "-c" ]
    env_file: .env
    command:
      - |-
        java -jar ./ch-simple/simple/target/simple-0.8-SNAPSHOT.jar