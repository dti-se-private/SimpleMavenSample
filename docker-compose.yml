services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${APP_PORT}:8000"
    entrypoint: [ "/bin/sh", "-c" ]
    env_file: .env
    command:
      - |-
        java -jar ./ch-simple/simple/target/simple-0.8-SNAPSHOT.jar